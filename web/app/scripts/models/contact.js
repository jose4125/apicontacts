/*global define*/

define([
    'underscore',
    'backbone'
], function (_, Backbone) {
    'use strict';

    var ContactModel = Backbone.Model.extend({
        
    });

    return ContactModel;
});